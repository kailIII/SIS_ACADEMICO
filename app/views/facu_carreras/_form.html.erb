<%= form_for(@facu_carrera) do |f| %>
  <% if @facu_carrera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facu_carrera.errors.count, "error") %> prohibited this facu_carrera from being saved:</h2>

      <ul>
      <% @facu_carrera.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :carrera_id %><br>
    <%= f.text_field :carrera_id %>
  </div>
  <div class="field">
    <%= f.label :facultad_id %><br>
    <%= f.text_field :facultad_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
