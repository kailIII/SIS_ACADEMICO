<h1>Listing nota</h1>

<table>
  <thead>
    <tr>
      <th>Nro nota</th>
      <th>Letra nota</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @nota.each do |notum| %>
      <tr>
        <td><%= notum.nro_nota %></td>
        <td><%= notum.letra_nota %></td>
        <td><%= link_to 'Show', notum %></td>
        <td><%= link_to 'Edit', edit_notum_path(notum) %></td>
        <td><%= link_to 'Destroy', notum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Notum', new_notum_path %>
