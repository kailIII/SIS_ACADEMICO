<%= form_for(@notum) do |f| %>
  <% if @notum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notum.errors.count, "error") %> prohibited this notum from being saved:</h2>

      <ul>
      <% @notum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nro_nota %><br>
    <%= f.text_field :nro_nota %>
  </div>
  <div class="field">
    <%= f.label :letra_nota %><br>
    <%= f.text_field :letra_nota %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
